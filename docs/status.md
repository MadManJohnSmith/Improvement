# Estado de la distribución

La arquitectura de destino completa se conserva en [ARQUITECTURA_FLUJO_AGENTES.md](../ARQUITECTURA_FLUJO_AGENTES.md). El progreso compacto por etapas está en [PLAN_IMPLEMENTACION.md](../PLAN_IMPLEMENTACION.md). Este archivo registra evidencia y límites comprobados; no convierte el diseño en capacidad disponible.

## Entregado y comprobado localmente

- Arquitectura normativa completa, separada de historial y hechos privados.
- Inicializador Python sin dependencias: dry-run, creación explícita, perfil mínimo y repetición sin sobrescribir; rechazo de rutas internas, traversal, enlaces, identidades inválidas y conflictos.
- `python3 -B -m unittest discover -s tests -v`: **17 tests aprobados** con múltiples aserciones en directorio temporal; producto sintético conservado, permisos de workspace comprobados. No es una prueba de sandbox, carreras hostiles ni recuperación tras corte eléctrico.
- Siete skills con frontmatter: cuatro procedimientos generales, incorporación y dos entradas operativas. Carga determinista de las siete mediante registro y `ctx.tools.execute` nativos del runtime instalado 0.1.5-rc.1, en alcance compartido de las dos filas de skills leídas del preset Standard instalado: dos agentes con padres distintos, ocho entradas por padre (siete copiadas y una exclusiva), cuerpos distintos bajo un mismo nombre, origen lógico/destino real y cuerpo exacto comprobados; volver al primer agente no filtra caché. Invocación de la skill exclusiva del otro padre rechazada. Inicializador real con enlaces gestionados; no se monta el Host/Standard completo. Regresión `tests/test_dsh_skill_root.mjs`, sin modelos. Además, trayectoria real de Standard revisada: carga nativa de `workflow-bounded-acceptance` desde el workspace externo y tres turnos sobre un caso sintético: aceptación con mejora opcional, retención por cita inventada y cierre tras corrección. Un cuarto turno ante resultados de tests requeridos pero ausentes devolvió UNVERIFIED, sin confundir ausencia con tests fallidos y solicitando evidencia ligada al candidato. Sin llamadas de escritura en esos turnos. Valida estos casos guiados, no todas las skills ni autonomía; fuentes estipuladas, sin verificación externa de citas. Historial privado conservado fuera del repositorio.
- Archivo original movido íntegramente a directorio hermano privado, con mapeo externo. Repositorios anidados y candidatos conservados; ningún archivo histórico forma parte del staging publicable.

## Operación acotada v0.1 comprobada

- START despacha Auditor y Reparación continua a skills nativas; guía de uso y recibos locales task/result con evidencia externa. No hay runtime nuevo ni integración del publicador histórico.
- Copia recursiva de skills, repetición idéntica idempotente y actualización explícita con respaldo de ediciones locales; enlaces/conflictos rechazados antes de copiar. Exposición nativa adicional mediante enlaces individuales gestionados en el padre, sin cambiar cwd ni configuración global. Manifiesto propio, rechazo de entradas ajenas/alteradas/ancestro Git y retirada que conserva copias y entradas ajenas comprobados.
- Esquema bash instalado inspeccionado; validador real comprueba omisión ordinaria, justificación requerida y rechazo de escalada no estrictamente mayor. START/Auditor distinguen error de argumentos de denegación y retienen sin granularidad de lectura. No hay guard nuevo ni prueba de cumplimiento LLM de estas instrucciones. El incidente fue comunicado por el mantenedor; no se recuperó su traza privada en esta unidad.
- `tests/test_operational_cycle.py`: ejecución real en fixture temporal de reproducción roja, candidato corregido y prueba verde; rechazo de candidato obsoleto, stdout alterado, QA ausente/mismo autor, comando no permitido, salida dentro del candidato, timeout y cambios fuera de `change_scope` (incluido alcance ausente). También comprueba `reaudit`, que liga mecánicamente resultado, prueba y candidato y conserva la semántica como pendiente. Producto sintético conservado. QA del fixture es simulada y no demuestra calidad/independencia de un agente.
- Registro y herramienta DSH instalados cargaron los siete cuerpos vigentes, incluidas ambas entradas; sin llamadas a modelos. Trayectoria Standard posterior verificada en un caso sintético: Reparación continua cargó ambas skills, ejecutó prueba verde con cambio fuera de `change_scope`, rechazó `ACCEPTED` y conservó `RETAINED`; sin cambios de repositorios. No se ensayaron estas entradas con QA semántica multimodelo, producto real ni agentes remotos.
- Unidad externa H1-only de QueueItem separada de la expansión retenida: `change_scope` único `ui/src/api/types.ts`, reproducción base roja (`exit_code 1`) y candidato H1-only verde (`exit_code 0`, 35 campos). `missions.py verify` conserva `RETAINED` por ausencia de QA independiente observable; reauditoría mecánica de hashes y alcance queda `UNVERIFIED`. No se modificaron Syncify ni este framework.
- El plan original archivado se reconcilió con el estado vigente: publicador Host, handoff automático, flujo habitual, recuperación, integración y transferencia a segundo proyecto siguen siendo destino o pendientes, no capacidades acreditadas. Los ensayos históricos de publicador/handoff/piloto Syncify son evidencia externa supervisada; no hubo llamadas LLM, IPC Tauri nativo, tests Rust del producto, integración ni prueba de sandbox OS fuerte.
- Etapa 2 avanzó a `PARCIAL`: `scripts/publisher.py` implementa un almacén externo mínimo con identidad/autorización suministradas, ID opaco, límites de payload/recibo/registros, creación exclusiva mediante temporal y enlace sin sobrescritura, reintento idempotente, conflicto y digest de contenido; `tests/test_publisher.py` comprueba resolución en proceso nuevo, corrupción y límites. No acredita autenticidad fuerte, Host, sandbox ni durabilidad ante crash/pérdida de energía.
- Etapa 4 avanzó dentro de la misma primitive: `Publisher.open_batch()`, `publish_batch_record()` y `close_batch()` permiten entrega local completa/parcial con owner, referencias hashadas y gaps; `report_stage()` modela executor/QA/auditor, exige `actor_id` distinto por rol y bloquea `ACCEPTED` sin QA `VERIFIED`; `bind_role()` conserva un mapa técnico declarativo, probado de forma aislada porque la propiedad local aún es de un solo caller. El cierre es idempotente y `resolve_handoff()` valida referencias desde otro proceso. La separación declarada no acredita independencia estadística, autenticidad fuerte, autorización Host multi-actor ni continuación autónoma.
- Regresión combinada `tests/test_two_mode_cycle.py`: onboarding externo, Auditor de fixture, Reparación roja→verde, QA fixture, `verify`, `reaudit` mecánica, entrega/estados y handoff local, comprobando que el producto sintético no cambia. Demuestra preparación mecánica del ciclo, no piloto Syncify, QA real, IPC Tauri, Host, sandbox ni recuperación.
- `missions.py reaudit` exige ahora `check_ref` íntegro para repair y valida timeout, argv autorizado, salida y hashes antes de emitir `PENDING_SEMANTIC_REVIEW`; no sustituye la revisión semántica.

- Correcciones locales: entrega `COMPLETE`, cierre sin nuevas escrituras, temporal exclusivo propio, rechazo de symlinks y etapas ligadas a artefactos de misión hashados con `verify`. Reauditoría reutiliza `verify`; regresiones incluyen evidencia borrada y actor auditor igual al ejecutor. La revisión independiente 1 señaló cinco defectos reproducibles, corregidos localmente: check opcional de audit validado si se aporta, sufijos de ciclo reservados, ID de lote limitado a 53 caracteres para admitir registros generados y etapas, esquema COMPLETE revalidado por el consumidor y rechazo de JSON no objeto. Regresiones dirigidas y suite Python completas aprobadas; son autopruebas de corrección, no revisión independiente 2 ni QA semántica.

- Correcciones de revisión 2: sufijos terminales reservados también entre lotes solapados (`b`/`b-x`), cuota de 16 con reserva persistente reconstruida para cierre y etapas (también ante publicaciones genéricas), y `ValueError` para IDs/enlaces anidados inválidos. Tres regresiones nuevas y ciclo positivo executor→QA→auditor con cuota al límite; 16 tests Python aprobados. Política conservadora documentada en arquitectura; autopruebas, no revisión independiente posterior ni garantías de concurrencia.

- Cierre de las tres revisiones independientes efectivamente realizadas: **5 / 3 / 1 hallazgos**, respectivamente, encontrados y corregidos (nueve posteriores a las seis correcciones originales). La última corrección valida payloads anidados de evidencia y tareas antes del cierre inmutable, coherente con el consumidor. Regresión de todos los tipos JSON no objeto y campo ausente: rechazo sin mutación, corrección con COMPLETE consumible o salida PARTIAL posterior. Suite final: **17 tests Python aprobados**; regresión Node nativa aprobada con runtime instalado y configuración explícita, dos padres, cuerpos exactos y rechazo entre proyectos, sin modelos ni instalación. Reproducciones originales 2/3 se detienen ahora en los rechazos corregidos; no equivalen a ejecuciones completas aprobadas. Son autocomprobaciones tras la tercera revisión, **no una cuarta revisión**, QA semántica real ni autonomía de producto.

## Antecedentes externos, no pruebas de esta distribución

El kit previo contiene publicador/resolver Cordis y ensayos deterministas reportados, fixture de contrato y QA del piloto. Su lector está ligado a tres archivos de un caso, sus bindings y scripts tienen alcance de ensayo; no se distribuye como adaptador universal. Se preservó todo el código en el archivo externo para reutilización dirigida, no se reconstruyó ni se ejecutó. El enlace histórico de dependencias al producto permanece intacto; el archivo no es operativo.

El paquete instalado DSH 0.1.5-rc.1 fue inspeccionado y sus servicios nativos ejecutados con Node 26.8.1. Ver setup-dsh.md para alcance y reproducción.

## Evidencia externa reciente

- Syncify `syncify-audit-001`: 8 unidades, base `9c9711e`, 73 reparaciones con recibos `ACCEPTED` y 15 hallazgos retenidos por decisión.
- RehabWeb `rehabweb-audit-001`: auditoría completa propia sobre base `5e638f0`, 6 unidades y 381 archivos asignados sin solapamiento; cola externa de 52 hallazgos creada. La primera pasada de reparación solo produjo propuestas/estados unitarios (`DONE`, `ACCEPTED`, `COMPLETED`) sin candidato, green y QA independiente uniformes; por contrato no se marca ningún hallazgo como reparado. Productos originales permanecen limpios.
- El launcher operativo externo fue corregido para derivar el workspace desde `dshHome` y usar el nombre del proyecto en el prompt; no modifica el runtime DSH ni plugins.

## Pendiente

El contrato v0.1 queda cerrado como operación acotada, no como autonomía. El framework añade contratos locales para capability externa, partición/consolidación de auditoría, cola CLI y archivo circular compacto; estas funciones validan artefactos, pero no conceden permisos ni sustituyen al Host. El piloto real externo de Syncify quedó `RETAINED`: onboarding e inventario pasaron; Cargo alcanzó el checkout real pero `frontendDist`/proc-macro bloqueó compilación y UI no tenía `node_modules`. Falta cargar el preset neutral mediante Creator/DSH, resolver dependencias con autorización y demostrar subagentes/QA efectivos. Presets dedicados activos, continuación Host, QA automática, límites durables, recuperación y transferencia a segundo proyecto real siguen pendientes hasta evidencia externa. El publicador histórico no se necesita para recibos locales de una unidad. No hubo llamadas de modelos, instalación, cambios globales ni QA independiente en esta reorganización.

Remoto origin configurado; publicación sujeta a autorización y autenticación efectiva. El repositorio es canónico: artefactos de ejecución y datos privados permanecen fuera del árbol. `AGENTS.md` establece las reglas de mantenimiento y evaluación con DSH Standard. Licencia pública pendiente del titular. La preparación para GitHub no equivale a publicación ni a motor completo.
