# Estado de la distribución

La arquitectura de destino completa se conserva en [ARQUITECTURA_FLUJO_AGENTES.md](../ARQUITECTURA_FLUJO_AGENTES.md). El progreso compacto por etapas está en [PLAN_IMPLEMENTACION.md](../PLAN_IMPLEMENTACION.md). Este archivo registra evidencia y límites comprobados; no convierte el diseño en capacidad disponible.

## Síntesis vigente — 2026-09-13

Las secciones posteriores conservan evidencia por hito; sus cifras históricas no se suman ni describen por sí solas el estado actual. Esta síntesis y el plan por etapas prevalecen para interpretar progreso.

- Arquitectura v2.4 y ocho skills distribuibles. Se incorporan memoria de decisiones, delegación/contención de contexto, métricas finas, doble perspectiva por unidad, barrido independiente antes de reconciliar, prevención de recurrencia y verificación humana necesaria. Son contratos/instrucciones; no nuevos servicios Host ni garantía de cumplimiento LLM. No se actualizaron las copias operativas de proyectos.
- `scripts/audit.py` añade inventario determinista SHA-256 con exclusiones justificadas, revalidación contra archivos actuales y `coverage-matrix.json` de partición. Rechaza snapshot obsoleto y asignación fuera del alcance; conserva `UNVERIFIED` con archivos omitidos, inventario ausente o unidades no aceptadas. Legacy sin snapshot conserva cola pero no acredita cobertura. `PARTITION_VERIFIED` no acredita lectura, doble revisión ni calidad semántica. Guía: [audit-contract.md](audit-contract.md).
- Comprobación de mantenimiento: **48 tests Python aprobados** con runtime instalado y fixtures externos, incluidos diez nuevos de cobertura. Se excluye expresamente `test_dsh_launchers.py`, que depende de lanzadores personales externos; no se presenta como discovery completo. Carga nativa de los cuerpos de las ocho skills aprobada desde dos padres con rechazo cruzado, sin modelos. El primer intento de prueba CLI falló por `APPIMAGE` heredado; repetido con `APPIMAGE`/`ARGV0` ausentes en el proceso de pruebas. No se modificó configuración del sistema para resolverlo.
- Launcher y broker acotados, presets externos y selección de rutas comprobados en sondas. El canal admite varias requests por lanzamiento y el tope de mensajes es configurable (64 por defecto, hasta 256); la regresión de 130 mensajes pasa con cuota elevada y se deniega con el default. No concede capacidad general de red ni mide coste monetario de misión.
- Presupuesto persistente y recuperación comprobados sobre fixtures: siete pruebas de ledger, reanudación en transiciones, cancelación, timeout y caída/reinicio local. No demuestran controlador Host real, recuperación de descendientes remotos ni autonomía LLM. El presupuesto del broker por lanzamiento sigue separado del ledger durable.
- Syncify: nueve cierres `ACCEPTED` (ocho lotes y una unidad previa) agrupan 73 fingerprints únicos; **no son 73 recibos individuales ni 73 reparaciones semánticamente verificadas**. Quince hallazgos históricos requieren decisión. Dos revisiones declaran alcance documental o sin semántica de parches; no se recuperaron revisiones QA originales compactas del conjunto. Un helper temporal usado por la unidad previa ya no está disponible para repetirla exactamente.
- RehabWeb: seis reparaciones con task/result/check/QA referenciados, rojo 1/verde 0 y respuestas originales de QA de sesiones distintas; se excluye un séptimo `ACCEPTED` duplicado e incompleto. Pruebas principalmente estáticas/configuración y una sobre fixtures, no build ni aceptación funcional integral. Tres casos auth necesitaron reejecución de pruebas host-side por problemas de quoting. Candidatos separados, sin integración conjunta ni cambio del producto original.
- Los seis `reaudit.json` de RehabWeb conservan `PENDING_SEMANTIC_REVIEW`; QA previa no equivale a Auditor posterior. Hashes de task/QA/check/informe coherentes en los quince cierres de ambos proyectos y enlaces de reauditoría coherentes en los seis; esta revisión de estado no reejecutó `verify` contra todos los candidatos/stdout. El checkpoint declara 44 ready y 4 retained; el conteo inicial de 52 y el posterior de 53 propuestas no permiten deducir reparaciones por resta sin reconciliar transiciones.
- En RehabWeb, 381 archivos están asignados sin solapamiento; los resultados recuperados enumeran 184 entradas de inspección, 155 rutas únicas y tres entradas no legibles. La asignación no acredita inspección de 381/381. Los resúmenes presupuestarios recuperados registran 73 y 218 requests en dos lotes, las últimas con 13 denegadas; no respaldan la cifra anterior de 169 productivas ni un coste total comparable.
- Carga nativa demostrada en sondas de Host, no ligada a los recibos de las misiones de producto. El último caso F6 retenido por mensajes requiere un nuevo ensayo después de la corrección de cuota; cambiar el broker no levanta esa retención. Todas las etapas globales permanecen `PARCIAL`: hay capacidades reales acotadas, no ciclo usuario-real completo autónomo.

## Entregado y comprobado localmente

- Arquitectura normativa completa, separada de historial y hechos privados.
- Inicializador Python sin dependencias: dry-run, creación explícita, perfil mínimo y repetición sin sobrescribir; rechazo de rutas internas, traversal, enlaces, identidades inválidas y conflictos.
- `python3 -B -m unittest discover -s tests -v`: **17 tests aprobados** con múltiples aserciones en directorio temporal; producto sintético conservado, permisos de workspace comprobados. No es una prueba de sandbox, carreras hostiles ni recuperación tras corte eléctrico.
- Siete skills con frontmatter: cuatro procedimientos generales, incorporación y dos entradas operativas. Carga determinista de las siete mediante registro y `ctx.tools.execute` nativos del runtime instalado 0.1.5-rc.1, en alcance compartido de las dos filas de skills leídas del preset Standard instalado: dos agentes con padres distintos, ocho entradas por padre (siete copiadas y una exclusiva), cuerpos distintos bajo un mismo nombre, origen lógico/destino real y cuerpo exacto comprobados; volver al primer agente no filtra caché. Invocación de la skill exclusiva del otro padre rechazada. Inicializador real con enlaces gestionados; no se monta el Host/Standard completo. Regresión `tests/test_dsh_skill_root.mjs`, sin modelos. Además, trayectoria real de Standard revisada: carga nativa de `workflow-bounded-acceptance` desde el workspace externo y tres turnos sobre un caso sintético: aceptación con mejora opcional, retención por cita inventada y cierre tras corrección. Un cuarto turno ante resultados de tests requeridos pero ausentes devolvió UNVERIFIED, sin confundir ausencia con tests fallidos y solicitando evidencia ligada al candidato. Sin llamadas de escritura en esos turnos. Valida estos casos guiados, no todas las skills ni autonomía; fuentes estipuladas, sin verificación externa de citas. Historial privado conservado fuera del repositorio.
- Archivo original movido íntegramente a directorio hermano privado, con mapeo externo. Repositorios anidados y candidatos conservados; ningún archivo histórico forma parte del staging publicable.

## Operación acotada v0.1 comprobada

- START despacha Auditor y Reparación continua a skills nativas; guía de uso y recibos locales task/result con evidencia externa. No hay runtime nuevo ni integración del publicador histórico.
- Copia recursiva de skills, repetición idéntica idempotente y actualización explícita con respaldo de ediciones locales; enlaces/conflictos rechazados antes de copiar. Exposición nativa adicional mediante enlaces individuales gestionados en el padre, sin cambiar cwd ni configuración global. Manifiesto propio, rechazo de entradas ajenas/alteradas/ancestro Git y retirada que conserva copias y entradas ajenas comprobados.
- Esquema bash instalado inspeccionado; validador real comprueba omisión ordinaria, justificación requerida y rechazo de escalada no estrictamente mayor. START/Auditor distinguen error de argumentos de denegación y retienen sin granularidad de lectura. No hay guard nuevo ni prueba de cumplimiento LLM de estas instrucciones. El incidente fue comunicado por el mantenedor; no se recuperó su traza privada en esta unidad.
- `tests/test_operational_cycle.py`: ejecución real en fixture temporal de reproducción roja, candidato corregido y prueba verde; rechazo de candidato obsoleto, stdout alterado, QA ausente/mismo autor, comando no permitido, salida dentro del candidato, timeout y cambios fuera de `change_scope` (incluido alcance ausente). También comprueba `reaudit`, que liga mecánicamente resultado, prueba y candidato y conserva la semántica como pendiente. Producto sintético conservado. QA del fixture es simulada y no demuestra calidad/independencia de un agente.
- Registro y herramienta DSH instalados cargaron los siete cuerpos vigentes, incluidas ambas entradas; sin llamadas a modelos. Trayectoria Standard posterior verificada en un caso sintético: Reparación continua cargó ambas skills, ejecutó prueba verde con cambio fuera de `change_scope`, rechazó `ACCEPTED` y conservó `RETAINED`; sin cambios de repositorios. No se ensayaron estas entradas con QA semántica multimodelo, producto real ni agentes remotos.
- Unidad externa H1-only de QueueItem separada de la expansión retenida: `change_scope` único `ui/src/api/types.ts`, reproducción base roja (`exit_code 1`) y candidato H1-only verde (`exit_code 0`, 35 campos). `missions.py verify` conserva `RETAINED` por ausencia de QA independiente observable; reauditoría mecánica de hashes y alcance queda `UNVERIFIED`. No se modificaron Syncify ni este framework.
- El plan original archivado se reconcilió con el estado vigente: publicador Host, handoff automático, flujo habitual, recuperación, integración y transferencia a segundo proyecto siguen siendo destino o pendientes, no capacidades acreditadas. Los ensayos históricos de publicador/handoff/piloto Syncify son evidencia externa supervisada; no hubo llamadas LLM, IPC Tauri nativo, tests Rust del producto, integración ni prueba de sandbox OS fuerte.
- Etapa 2 avanzó a `PARCIAL`: `scripts/publisher.py` implementa un almacén externo mínimo con identidad/autorización suministradas, ID opaco, límites de payload/recibo/registros, creación exclusiva mediante temporal y enlace sin sobrescritura, reintento idempotente, conflicto y digest de contenido; `tests/test_publisher.py` comprueba resolución en proceso nuevo, corrupción y límites. No acredita autenticidad fuerte, Host, sandbox ni durabilidad ante crash/pérdida de energía.
- Etapa 4 avanzó dentro de la misma primitive: `Publisher.open_batch()`, `publish_batch_record()` y `close_batch()` permiten entrega local completa/parcial con owner, referencias hashadas y gaps; `report_stage()` modela executor/QA/auditor, exige `actor_id` distinto por rol y bloquea `ACCEPTED` sin QA `VERIFIED`; `bind_role()` conserva un mapa técnico declarativo, probado de forma aislada porque la propiedad local aún es de un solo caller. El cierre es idempotente y `resolve_handoff()` valida referencias desde otro proceso. La separación declarada no acredita independencia estadística, autenticidad fuerte, autorización Host multi-actor ni continuación autónoma.
- Regresión combinada `tests/test_two_mode_cycle.py`: onboarding externo, Auditor de fixture, Reparación roja→verde, QA fixture, `verify`, `reaudit` mecánica, entrega/estados y handoff local, comprobando que el producto sintético no cambia. Demuestra preparación mecánica del ciclo, no piloto Syncify, QA real, IPC Tauri, Host, sandbox ni recuperación.
- `missions.py reaudit` exige ahora `check_ref` íntegro para repair y valida timeout, argv autorizado, salida y hashes antes de emitir `PENDING_SEMANTIC_REVIEW`; no sustituye la revisión semántica.

- Correcciones locales: entrega `COMPLETE`, cierre sin nuevas escrituras, temporal exclusivo propio, rechazo de symlinks y etapas ligadas a artefactos de misión hashados con `verify`. Reauditoría reutiliza `verify`; regresiones incluyen evidencia borrada y actor auditor igual al ejecutor. La revisión independiente 1 señaló cinco defectos reproducibles, corregidos localmente: check opcional de audit validado si se aporta, sufijos de ciclo reservados, ID de lote limitado a 53 caracteres para admitir registros generados y etapas, esquema COMPLETE revalidado por el consumidor y rechazo de JSON no objeto. Regresiones dirigidas y suite Python completas aprobadas; son autopruebas de corrección, no revisión independiente 2 ni QA semántica.

- Correcciones de revisión 2: sufijos terminales reservados también entre lotes solapados (`b`/`b-x`), cuota de 16 con reserva persistente reconstruida para cierre y etapas (también ante publicaciones genéricas), y `ValueError` para IDs/enlaces anidados inválidos. Tres regresiones nuevas y ciclo positivo executor→QA→auditor con cuota al límite; 16 tests Python aprobados. Política conservadora documentada en arquitectura; autopruebas, no revisión independiente posterior ni garantías de concurrencia.

- Cierre de las tres revisiones independientes efectivamente realizadas: **5 / 3 / 1 hallazgos**, respectivamente, encontrados y corregidos (nueve posteriores a las seis correcciones originales). La última corrección valida payloads anidados de evidencia y tareas antes del cierre inmutable, coherente con el consumidor. Regresión de todos los tipos JSON no objeto y campo ausente: rechazo sin mutación, corrección con COMPLETE consumible o salida PARTIAL posterior. Suite final: **17 tests Python aprobados**; regresión Node nativa aprobada con runtime instalado y configuración explícita, dos padres, cuerpos exactos y rechazo entre proyectos, sin modelos ni instalación. Reproducciones originales 2/3 se detienen ahora en los rechazos corregidos; no equivalen a ejecuciones completas aprobadas. Son autocomprobaciones tras la tercera revisión, **no una cuarta revisión**, QA semántica real ni autonomía de producto.

## Hito 1 de mantenimiento Host externo — 2026-09-12

- `scripts/host_launcher.py` añade lanzamiento Linux/bubblewrap fail-closed, sin fallback libre: filesystem vacío salvo plataforma `/usr` y compatibilidad bin/lib en lectura, entradas explícitas `--read NAME=ABSOLUTE_PATH` en `/inputs/NAME`, proc/dev privados, red/PID/IPC aislados, entorno reconstruido y raíz de estado nueva 0700 externa en `/state`. HOME, DSH_HOME, XDG y tmp son privados; no hereda sockets, credenciales ni configuración del Host activo. Rechaza sockets en entradas, enlaces en rutas raíz, solapamiento de estado y lecturas amplias conocidas. El caller debe autorizar entradas sin secretos y mantenerlas estables; no hay filtrado semántico de credenciales dentro de una lectura autorizada ni garantía contra mutaciones concurrentes hostiles.
- Uso: `python3 -B scripts/host_launcher.py --read node_modules=/ruta/runtime/node_modules --read product=/ruta/fixture --state-parent /ruta/estado-externo --cwd /ruta/fixture -- /usr/bin/node /inputs/node_modules/@deepseek-ai/dsh/lib/bin.js --profile headless --help`. El padre de estado debe existir y no solaparse con entradas. Cada intento conserva stdout/stderr fuera del repo; no reutiliza estado ni instala/configura modelos. El nombre `node_modules` preserva resolución nativa de dependencias.
- Una regresión nueva, `tests/test_host_launcher.py`, reutiliza el ensayo externo previo con servicios instalados Cordis/fs/bash/subprocess/sandboxPolicy: lectura autorizada, escritura externa, denegación de producto/framework incluso con `danger-full-access`, traversal, symlink y shell descendiente; entorno limpio, secreto sintético no montado, conexión al listener TCP exterior denegada, raíces nuevas, socket Unix rechazado y ejecutable ausente sin fallback. Fixtures y evidencia en `/tmp`; `DSH_MODULE_ROOT` explícito, omitida si no se aporta runtime, nunca instala. Suite ejecutada con runtime: **23 tests Python aprobados**, más regresión Node nativa aprobada.
- Prueba externa separada aprobada: composición web distribuida arrancó con raíz Cordis activa y listener privado/puerto efímero. El 401 inicial era autenticación esperada, no bloqueo material: un cliente local con cookie firmada exclusivamente mediante el secreto generado por esa instancia obtuvo **HTTP 200 y HTML válido**, creó sesión Standard y consultó `skills/list`. Con `ctx.agents.get(sessionId)` y `ctx.tools.execute` nativo se cargaron `workflow-auditor` y `workflow-continuous-repair`, comprobando proveedor filesystem, origen explícito del workspace privado y cuerpos exactos frente a las skills autorizadas; no solo catálogo ni scopes de fixture. El Host se cerró. CLI headless `--help` también terminó con código 0. Esto acredita arranque autenticado y carga nativa en sesión Host **sin modelos**, no ejecución conductual de los modos, QA, subagentes remotos ni continuación autónoma. No se modificaron runtime instalado, Host activo, productos, routing ni credenciales activas; los secretos desechables no se imprimen en el ensayo autenticado.
- El launcher conserva ahora el **cwd original** de una entrada de directorio autorizada mediante bind adicional de solo lectura; sus padres se crean vacíos, no se montan. `/tmp` es un bind privado, no un symlink que altere `getcwd`. Rechaza colisiones con raíces reservadas. La misma regresión acredita cwd exacto de Node y bash, lectura relativa, denegación por ruta original y secreto hermano oculto. Suite final: **23 tests Python y check Node aprobados**; ensayo Host autenticado repetido con el launcher corregido.
- Integración de inferencia acotada implementada: `scripts/inference_channel.py` usa stdlib, dos pipes anónimos y HTTP loopback interno al namespace bwrap; allowlist exacta del endpoint local `/v1/chat/completions`, método POST, modelo fijado, payload/respuesta limitados, deadline, sin redirects, headers entrantes, endpoints administrativos, sockets host, red compartida ni RPC general. `launch(..., inference=...)` monta solo el broker en lectura; autenticación se conserva en memoria host-side y no entra en argv, entorno o lecturas del agente. `tests/test_inference_channel.py` cubre fixture aislado, rechazo de URLs/path/query/modelo/headers, cuota, redirects, respuestas grandes, secretos y deadline; suite completa ejecutada externamente con runtime instalado. Una inferencia mínima real autorizada en Host web desechable conservó `nrouter`/`Orquestrador` y recibió `OK` (`text-delta`, `finish stop`); evidencia externa en `/tmp/inference-real-maintenance/runs/host-ws8iyzrv/inference-result.json`. No se cambiaron alias/routing activos, runtime, red general ni credenciales. Esto acredita el canal y una llamada, no conducta de modos, subagentes, QA ni autonomía.
- Aislamiento temporal del bloqueo real: `nrouter/Orquestrador` devolvió `EMPTY_RESPONSE` con `responseModel=muse-spark-1.3-contributor-free` y sin `text-delta` (`/tmp/inference-real-maintenance/runs/host-h7ehr9wk/inference-result.json`). Una selección explícita ya permitida por Host, `nrouter/ag/gemini-3.8-flash-high`, produjo `text-delta=OK`, `finish=stop`, `responseModel=gemini-3.8-flash`, `rc=0` (`/tmp/inference-real-maintenance/runs/host-vbj6tpb_/inference-result.json`). Se preservó el routing persistente; esto diferencia modelo/alias upstream de broker, pero no acredita aún Syncify, subagentes ni QA real.

## Presets externos del flujo — 2026-09-12

- Catálogo no secreto del runtime (settings real, sin abrir `.credentials.yaml`): proveedor NORMAL `nrouter` (modelos `ag/...` y `Subagents-Coding`) y RAW `nrouter-raw` (`Subagents-Audits`, `Subagents-Coding`, `Orquestrador`, `Subagents`, `ATM`), ambos openai-completions sobre `localhost:20128/v1`. Humo aislado por broker: `nrouter/Orquestrador`, `nrouter-raw/Subagents-Audits` y `nrouter/Subagents-Coding` devolvieron `OK` con `finish=stop`; el `EMPTY_RESPONSE` previo de Orquestrador no se reprodujo. Evidencia en `/tmp/dsh-workflow-presets/smoke-*.json`.
- Presets compuestos externamente en `/home/alan/DSH-workspace/workflow-presets` (`auditor-preset`, `repair-preset`; `preset.yml` + `agent.cordis.yml`): filas de delegación copiadas del Standard upstream (spawn con `modelSelectionSettings: true`, fork, control, workflow), filesystem/search/bash y skills nativas, sin tool-web ni declaración de proveedores o modelos; skills por origen explícito del workspace externo. `~/.dsh`, presets distribuidos y routing global sin cambios (hashes de settings y credenciales verificados antes y después).
- Límite comprobado del runtime: `allowedModels` vive en el ajuste global `subagent-model-selection` (namespace de registro único; un segundo registro lanza error). Una fila propia de `model-selection-settings` en un preset falla el montaje en voz alta, así que las rutas por preset o por rol no son expresables y la separación QA/ejecución es procedimental. La aceptación aislada lleva la lista autorizada por preset: auditor `[nrouter-raw/Subagents-Audits]`; repair `[nrouter/Subagents-Coding, nrouter-raw/Subagents-Audits]`.
- Aceptación por preset en Host aislado (launcher + patch con raíz de presets `trust: system`): sesión creada por preset; modelo efectivo de sesión `nrouter/Orquestrador`; proyección duradera de política exacta al mapeo; carga nativa de `workflow-complete-auditor`/`workflow-continuous-repair` con cuerpo exacto y origen del workspace montado; denegación fail-closed exacta (`nrouter/Subagents-Coding` en auditor y `nrouter/ag/gemini-3.8-flash-high` en repair → `not allowed for this Session`); `list_subagent_models` filtra por la lista autorizada. Evidencia en `/tmp/dsh-workflow-presets/accept-*.json`.
- Límite material del canal de humo: el turno de un agente hijo lleva la clave `tools` fuera de la allowlist del broker (`extraKeys:tools`), así que la completación completa del turno del hijo no puede pasar por este canal; la resolución de ruta y el enforcement quedan acreditados (todas las requests del spawn al broker llevaron el modelo autorizado y ninguna llevó una ruta no autorizada). Extender el canal no estaba autorizado en esta fase.
- Regresión `tests/test_dsh_preset_mapping.py`: fixture autofirmado que afirma el mapping efectivo (ruta de sesión y proyección igual a allowedModels), doble rechazo fail-closed (proveedor y modelo no listados) y rechazo de un preset con namespace propio. Suite completa: **27 tests Python aprobados** con runtime instalado.

## Corrección de mapeo de sesión a canal RAW — 2026-09-12

- Fila de sesión/orquestador corregida a canal RAW en ambas composiciones externas (`auditor-preset`, `repair-preset`): sesión/orquestador `nrouter-raw/Orquestrador`; sin cambios en QA (`nrouter-raw/Subagents-Audits`) ni ejecución (`nrouter/Subagents-Coding`, proveedor NORMAL). Catálogo no secreto verificado: `Orquestrador` figura bajo `nrouter-raw`.
- Re-aceptación aislada por preset (host_launcher + broker, settings desechables con `agent-default-model` RAW): sesión creada por preset con modelo efectivo `nrouter-raw/Orquestrador` (comprobación explícita `sessionRouteRawOrchestrator`), proyección de política exacta al mapeo por preset, skills `workflow-complete-auditor`/`workflow-continuous-repair` con cuerpo exacto, denegación fail-closed de rutas no listadas (`nrouter/Subagents-Coding` en auditor; `nrouter/ag/gemini-3.8-flash-high` en repair → `not allowed for this Session`) y ninguna ruta no autorizada alcanzó el broker. Evidencia en `/tmp/dsh-workflow-presets/accept-raw-auditor.json` y `accept-raw-repair.json`.
- Humo mínimo de la ruta de sesión corregida: `nrouter-raw/Orquestrador` devolvió `OK` con `text-delta`, `finish=stop` y rc 0; el `EMPTY_RESPONSE` previo de Orquestrador no se reprodujo en el canal RAW. Evidencia en `/tmp/dsh-workflow-presets/smoke-orquestador-raw.json` (estado `runs/host-lew4akfd`).
- El spawn autorizado siguió denegado por el broker (`extraKeys:tools`), límite conocido del canal que se aborda en la unidad de extensión del canal; rutas y enforcement sin cambios.

## Extensión del canal de inferencia para turnos de subagentes — 2026-09-12

- `scripts/inference_channel.py` extiende su allowlist fail-closed con las claves estructurales de turno que el runtime emite: `tools` y `tool_choice` en el payload y las formas de mensaje del turno con herramientas (`tool_calls` de assistant con `{id,type,function:{name,arguments}}`, resultado `role:"tool"` con `tool_call_id`), verificadas contra la API openai-completions del runtime instalado (`requiresToolResultName:false`, grammar desactivado: sin `name` de resultado ni entradas `custom`). Endpoint, modelo y autorización siguen fijos host-side; claves no listadas, formas malformadas y `tool_choice` fuera de `auto|none` se rechazan sin alcanzar el upstream. El presupuesto de una request por lanzamiento y las cuotas de tokens (1..32) y mensajes (1..8) no cambian.
- Regresión `tests/test_inference_channel.py::test_tool_turn_allowlist`: turno con `tools` aceptado y reenviado; segunda pierna del turno (assistant `tool_calls` + tool result) aceptada; claves no listadas (`logprobs`, `user`, `parallel_tool_calls`, `response_format`) y formas malformadas rechazadas sin tocar el upstream. Suite completa: **28 tests Python aprobados**.
- Verificación real en Host aislado (presets externos, sesión `nrouter-raw/Orquestrador`): el spawn autorizado `nrouter-raw/Subagents-Audits` completó su turno por el canal (`isError:false`, texto `OK.`) con `tools` declarado, una sola request al broker y ninguna ruta no autorizada; la denegación fail-closed de rutas no listadas se mantuvo. El presupuesto de una request por lanzamiento (`Request budget exhausted` observado cuando el hijo encadena una segunda completación), la cuota de tokens frente al default del hijo (32768; la sonda fijó 16 heredado) y el límite de mensajes por request quedaron levantados en la unidad siguiente. Evidencia en `/tmp/dsh-workflow-presets/accept-tools-auditor.json`.

## Canal de sesión multi-request — 2026-09-12

- `scripts/inference_channel.py` es ahora un canal de sesión acotado multi-request, sin proxy general: el presupuesto de requests se fija por lanzamiento al construir el canal (12 por defecto, tope duro 256) y el broker vive mientras viva el proceso de la misión; cuotas por request de hasta 32768 tokens (default del hijo, antes 32) y 64 mensajes (antes 8), rebajables por lanzamiento y nunca superables. Endpoint, modelo, autorización host-side y la allowlist fail-closed de claves (`tools`/`tool_choice`/`tool_calls`/`tool_call_id`) no cambian. Agotado el presupuesto, la única respuesta es un error explícito fail-closed (503 `Inference budget exhausted` en el canal y línea `FAIL CLOSED` en el log externo) y el canal queda cortado por el resto del lanzamiento; las requests posteriores no alcanzan el upstream. El broker aparta su capa de texto de stdout para que un corte de pipes no degrade el cierre (antes el intérprete salía 120 por un flush fallido).
- Regresiones nuevas en `tests/test_inference_channel.py`: multi-request dentro de presupuesto, agotamiento corta sin upstream, cuotas nuevas aplicadas en ambos topes y rebajadas por lanzamiento, validación fail-closed de argumentos de cuota, y el camino real por `host_launcher` con presupuesto 1 demuestra error 503 explícito, canal cortado (502) y línea `FAIL CLOSED` en stderr con un solo hit upstream. Suite completa: **29 tests Python aprobados** con runtime instalado.
- Sonda real en Host aislado (launcher + presets externos, sesión `nrouter-raw/Orquestrador`): el spawn autorizado `nrouter-raw/Subagents-Audits` completó un turno completo con herramientas mediante **2 requests** por el canal —primera con `tools` declarado y 4 mensajes, segunda con `tool_calls` de assistant y resultado `tool` (6 mensajes)—, con el default del hijo 32768 aceptado sin overrides de la sonda; presupuesto 12, gasto 2, ningún payload denegado y ninguna ruta no autorizada alcanzó el broker; la denegación de spawn no autorizado se mantuvo y el hijo respondió con la primera línea literal del README leído. Evidencia en `/tmp/inference-multi-request/probe-auditor.json`. Esto acredita el turno multi-request del hijo por el canal; no acredita QA real, misión completa ni autonomía, y el corte por agotamiento en Host real no se ejercitó con un hijo hostil (solo en regresión instrumentada).

## Límite de payload y partes de assistant en el canal — 2026-09-12

- `scripts/inference_channel.py`: payload por request ahora configurable por lanzamiento (`payload_limit`, 1 MiB por defecto, tope duro 8 MiB, validado como las demás cuotas) y acepta los campos textuales de razonamiento `reasoning`/`reasoning_content`/`reasoning_text` y el contenido de assistant como lista no vacía de partes exactas `{type:'text',text}` — formato verificado en la fuente del runtime instalado (`openai-completions.js`, `OPENAI_COMPLETIONS_REASONING_FIELDS`, rama `requiresThinkingAsText`) y en los transcritos del Host L1 de la misión RehabWeb (partes assistant `text`/`tool-call`, sin `reasoning_details`). Endpoint, modelo, autorización host-side, presupuesto de requests, cuotas de tokens/mensajes y el resto de la allowlist estructural sin cambios; `reasoning_details`, partes no textuales, claves no listadas y formas malformadas siguen rechazadas sin alcanzar el upstream.
- Motivo registrado: la unidad RehabWeb (RETAINED F6-DUPLICATE-CONVERSATION-500, transcritos de ~16 pasos, 56 requests, 0 denials de ruta) murió porque conversaciones mayores de 64 KiB recibían 400 fail-closed del receptor; el tope era la constante fija de 64 KiB. Ahora el tope se impone en las tres capas del lanzamiento (frame del pipe, predicate del broker y Content-Length del receptor) y el launcher comunica el valor en argv del receptor como entero no secreto (`scripts/host_launcher.py`).
- Regresiones nuevas en `tests/test_inference_channel.py`: payload >64 KiB aceptado con el default, denegado con tope rebajado por lanzamiento y sobre el tope duro incluso pidiéndolo, validación fail-closed del argumento, campos de razonamiento y partes textuales de assistant aceptados (incluida la segunda pierna con `tool_calls`), diez formas malformadas de reasoning/partes rechazadas, partes textuales de `user` seguidas denegadas, y camino real por `host_launcher` con payload grande (200) y tope rebajado (400 del receptor sin hit upstream). Suite completa: **31 tests Python aprobados** con runtime instalado.
- Sin sonda real adicional en esta unidad: el formato se comprobó contra la fuente del runtime instalado y los transcritos existentes; el relanzamiento de la unidad RehabWeb ejercitará el canal completo en real. Presupuesto/cuotas fijos host-side, sin proxy general; sin cambios de routing global, credenciales, productos ni presets distribuidos.

## Recuperación durable sobre fixtures — 2026-09-12

- `scripts/budget.py` añade el contrato local mínimo de límites durables (`attempts`/`cost`/`seconds`): ledger de eventos inmutables en estado externo con creación exclusiva, digest de contenido, reserva conservadora antes del trabajo y consumo recalculado desde los eventos; los límites se fijan una sola vez, la reapertura con límites distintos o el registro alterado fallan cerrados y el agotamiento (`BudgetExhausted`) no escribe. No mide procesos reales ni concede autoridad; almacén local de un solo escritor, sin garantía ante concurrencia hostil ni pérdida de energía.
- Regresión nueva `tests/test_recovery_durable.py` (7 tests, fixtures temporales en `/tmp`, sin modelos ni misión real): (1) operación normal continuada — una sola sesión consume 4 unidades de una cola fixture con presupuesto persistente, sin intervención, y una quinta unidad falla cerrada por agotamiento sin escrituras; (2) reinicio del controlador en los tres puntos de transición (durante QA, antes de publicar, después de publicar/antes de ack) con reconciliación idempotente: cero escritores duplicados (sonda de proceso externo: mismo contenido IDEMPOTENT, distinto CONFLICT), cero integración repetida (segunda pasada sin bytes nuevos), candidato conservado, presupuesto acumulado 2/2/… sin reinicio y rechazo de reenvío ciego con contenido distinto; (3) caída de worker (SIGKILL) en punto de transición con reanudación completa y restos temporales huérfanos inofensivos; (4) cancelación del controlador fixture: los descendientes locales mueren (evidencia por flock exclusivo liberado), estado PENDING recuperable y presupuesto conservado; (5) timeout de turno — `missions.run_check` mata el grupo del comando (flock liberado y PID ausente en `/proc`), la unidad queda RETAINED preservada y es recuperable tras decisión externa; (6) reinicio del Host con `host_launcher` — turno aislado cortado por timeout (124, `FAIL CLOSED`) en los tres puntos: el estado host-side (recibos, presupuesto, candidato) no se reinicia ni se publica a ciegas desde un turno muerto, cada lanzamiento recibe `/state` nueva, el sandbox no ve los recibos ni puede escribir el producto, y el QA recolectado queda invalidado si cambia el candidato (hash). Suite completa: **38 tests Python aprobados** con runtime instalado (`DSH_MODULE_ROOT`).
- Nivel de evidencia: los puntos de publicador, `run_check`, launcher y ledger son comportamiento del framework; el controlador de sesión fixture demuestra el contrato de reconciliación que deberá satisfacer el Host (fila 7 del plan), no el Host real. Quedan fuera de la capacidad aceptada y probada: descendientes remotos (fuera del namespace de PID local; la cancelación los deja huérfanos salvo control Host dedicado), autonomía LLM, QA semántica real, concurrencia hostil sobre el mismo ledger/recibos y durabilidad ante pérdida de energía.

## Onboarding de sesión, cuota de mensajes y reintento F6 — 2026-09-13

- Confirmación empírica del escenario conductual documentado en setup-dsh.md (escenario (2) «strictly wider», tercer caso observado y primero con transcript recuperable): la sesión usuario-real de la instancia RehabWeb 3081 con cwd `/home/alan/DSH-workspace` orchestrió sola, sin skills (cwd sin `.agents` gestionado) y su ejecutor chocó con el esquema de `bash` (`sandbox_permissions` obligatorio rechazado como «not strictly wider» sobre el mismo nivel; aprobación `danger-full-access` cancelada); el agente paró honestamente, sin recibos falsos ni cambios de producto (transcript bajo `workspaces/RehabWeb/dsh-home/.dsh/sessions/`). Corrección esperada registrada y línea mínima añadida al cuerpo de `workflow-continuous-repair`: ante el rechazo el ejecutor retiene con el error exacto, sin reintentar escaladas. Los escenarios (1) y (3) siguen pendientes de ejecución conductual.
- `scripts/onboard.py` incorporó `session-rehabweb-2` al estilo session-syncify: perfil nuevo `workspace/` (project=/home/alan/DSH-workspace/projects/RehabWeb, sin colisión con el perfil ni segundo backlog; `cycles/` intacto), copia de las 8 skills vigentes y enlaces gestionados con manifiesto en la raíz de sesión; dry-run antes de init. Estado ajeno encontrado y conservado: un enlace simbólico previo en `.agents/skills` hacia el workspace de Syncify bloqueaba el esquema gestionado (fallo cerrado del inicializador); se renombró a `.agents/skills.syncify-link.prev` sin descartarlo. Humo nativo sin modelos (patrón `test_dsh_skill_root.mjs`) sobre la raíz real: las 8 skills cargan con proveedor filesystem, realpath hacia la copia del workspace y cuerpo exacto; nombre inexistente sigue rechazado. El cwd `session-rehabweb-2` queda listo para una sesión usuario-real con skills nativas.
- `scripts/inference_channel.py`: el tope de mensajes por request pasó de constante fija 64 a cuota configurable por lanzamiento (`DEFAULT_MESSAGES=64`, tope duro `MAX_MESSAGES=256`, validación idéntica al resto de cuotas; presupuesto, tokens, payload, allowlist y fail-closed sin cambios). Motivo: en el lote 2 de RehabWeb el ejecutor murió con 12 denials fail-closed `messagesBounds` (messageCount máximo 66) pese a funcionar el payload ampliado. Regresiones nuevas en `tests/test_inference_channel.py`: default 64 acepta 64 y deniega 65, lanzamiento elevado acepta el tope duro y deniega 257, y camino real por `host_launcher` con 130 mensajes (502 al default, 200 con tope elevado, un solo hit upstream). Suite de módulos comprometidos: **39 tests Python aprobados** con runtime instalado; regresión Node `test_dsh_skill_root.mjs` aprobada.
- Reintento F6-DUPLICATE-CONVERSATION-500 en rig aislado (`mission-faseb-b3`, patrón de los lotes 1/2): L1 ejecutor `nrouter/Subagents-Coding` (presupuesto 256, mensajes 256, gasto 40, **0 denials**, messageCount máximo 82, payload máximo 198621 bytes) produjo candidato attempt-1 con parche de un archivo en `change_scope` y prueba Django real roja→verde (sqlite en memoria vía deps montadas); L2 QA `nrouter-raw/Subagents-Audits` en sesión separada (presupuesto 170, gasto 15) devolvió ACCEPTED con scope/test/criterio verificados. Recibos formato syncify en `repairs/F6-DUPLICATE-CONVERSATION-500/` (task, candidato en copia de ensayo, red/green, qa.json, result.json); `verify` ACCEPTED y `reaudit` mecánica OK. El run-check de recibos se ejecutó dentro del mismo sobre aislado (test.sh exige `/inputs/deps`); el intento host-side fallido por ese desfase se conserva como evidencia. Checkpoint actualizado (lote `faseb-003`: 1 ACCEPTED, 0 RETAINED; excepción F6 resuelta; 43 ready restantes). Producto original intacto (hashes del ciclo); integración solo en copia de ensayo, sin integrar al producto real.
- Nivel de evidencia: la aceptación de F6 es de unidad en rig aislado con QA subagente real, no integración autorizada al producto ni autonomía Host; la corrección conductual de la skill es una instrucción, no un guard; el humo de skills acredita carga nativa sin modelos, no comportamiento de modos.

## Registro de presets propios en la instancia RehabWeb 3081 — 2026-09-13

- Mecanismo mínimo verificado en fuente upstream y runtime instalado 0.1.5-rc.2: el roster de `agent-presets` descubre en vivo (relee las raíces en cada `list`) la raíz de usuario derivada `$DSH_HOME/.agent-presets` (trust `user`, `includeUserRoot` por defecto true; el bundle web solo fija `default: standard`), así que basta con crear los directorios de preset bajo esa raíz de la instancia: sin editar presets distribuidos, sin `settings.yaml` global y sin reinicio. Los symlinks de directorio se omiten en el escaneo (`dirent.isDirectory`), pero los archivos enlazados sí valen: los directorios propios contienen `agent.cordis.yml`/`preset.yml` como enlaces a `/home/alan/DSH-workspace/workflow-presets/{auditor-preset,repair-preset}`, fuente única sin copias que deriven. Ruta registrada: `/home/alan/DSH-workspace/workspaces/RehabWeb/dsh-home/.dsh/.agent-presets/{auditor-preset,repair-preset}`.
- Visibilidad en la instancia 3081 comprobada por RPC autenticado local (patrón del runner del mantenedor; secreto derivado en memoria, nunca impreso ni persistido): `agentPresets/list` devuelve ambas filas con trust `user` y sin `broken`, junto a los cuatro presets de sistema. Sin modelos ni turnos.
- Sesión de prueba creada en 3081 con `agentPreset: repair-preset` y cwd `/home/alan/DSH-workspace/session-rehabweb-2` (`session-d589d148-dd50-4107-b139-21c19071d36a`, cabecera durable con `agentPreset: repair-preset`), en blanco y sin `session/prompt`: `skills/list` carga las ocho skills del workspace (más la capa global), con `workflow-continuous-repair` presente. La sesión queda abrible por el usuario con Reparación continua.
- Comportamiento del subagente ejecutor en Host aislado (launcher, sin canal de inferencia ni modelos): mismo mecanismo de registro por raíz de usuario, sin patch de `agent-presets`; sesión con repair-preset sobre el workspace real montado de solo lectura; hijo ejecutor creado por la ventana nativa (`childSessionMeta` + `applyChildComposition`) hereda repair-preset y `bash`; una llamada bash ordinaria SIN `sandbox_permissions` dentro del workspace termina `isError:false` bajo modo `workspace-write` (enforcement `full`), y el esquema del hijo mantiene `sandbox_permissions` OPCIONAL (el fallo de producción previo era conductual, no de esquema). La sonda de escritura dentro del workspace falló por el sistema de archivos de solo lectura del montaje del launcher (`Read-only file system`, que el sandbox reporta como denegación bajo `workspace-write`): límite del aislamiento, no del preset. Evidencia en `/tmp/dsh-preset-3081-registration/` (`roster-scan.json`, `probe-userroot-result.json`, `session-3081-result.json`; script `check/probe-userroot.mjs`).
- Límites: la aceptación aislada no llama modelos ni ejercita turnos completos de subagente (eso corresponde a los lotes ya probados en rig); la denegación de escritura en el probe proviene del montaje de solo lectura, y el tratamiento conductual del rechazo de escalada sigue siendo instrucción de skill, no guard. La instancia 3080, credenciales, routing y productos no fueron modificados; la prueba en 3081 creó únicamente una sesión en blanco.

## Antecedentes externos, no pruebas de esta distribución

El kit previo contiene publicador/resolver Cordis y ensayos deterministas reportados, fixture de contrato y QA del piloto. Su lector está ligado a tres archivos de un caso, sus bindings y scripts tienen alcance de ensayo; no se distribuye como adaptador universal. Se preservó todo el código en el archivo externo para reutilización dirigida, no se reconstruyó ni se ejecutó. El enlace histórico de dependencias al producto permanece intacto; el archivo no es operativo.

El paquete instalado DSH 0.1.5-rc.1 fue inspeccionado y sus servicios nativos ejecutados con Node 26.8.1. Ver setup-dsh.md para alcance y reproducción.

## Evidencia externa inicial — alcance histórico

- Syncify `syncify-audit-001`: ocho unidades de auditoría, base `9c9711e`; el resumen histórico atribuyó 73 reparaciones. La revisión vigente distingue nueve cierres agrupados y sus límites QA, según la síntesis inicial de este archivo.
- RehabWeb `rehabweb-audit-001`: base `5e638f0`, seis unidades y 381 archivos asignados, cola inicial de 52 hallazgos. La primera pasada no acreditaba reparaciones uniformemente; seis cierres posteriores sí aportan cadenas de evidencia acotadas. No confundir estos momentos ni asignación con revisión completa.
- El launcher operativo externo fue corregido para derivar el workspace desde `dshHome` y usar el nombre del proyecto en el prompt; no modifica el runtime DSH ni plugins.

## Pendiente

El contrato v0.1 sigue siendo operación acotada, no autonomía. Hay presets y turnos reales comprobados en sondas, candidatos de dos proyectos y QA limitada de RehabWeb; ya no corresponde declarar todas esas capacidades ausentes. Falta ligarlas en una misión usuario-real completa: carga/aplicación nativa de skills, autorización Host multi-actor, consumo de cola, revisión semántica posterior, integración autorizada y cierre conjunto. Inventario/recibos/ledger validan propiedades locales, no conceden permisos ni sustituyen al Host. La recuperación y el presupuesto durable están probados en fixtures, no integrados con todas las inferencias ni descendientes remotos. Memoria de decisiones, evidencia humana y métricas finas tienen contrato normativo; su instrumentación y enforcement siguen pendientes. El publicador histórico permanece como antecedente de reutilización dirigida, no dependencia de la unidad local.

Remoto origin configurado; publicación sujeta a autorización y autenticación efectiva. El repositorio es canónico: artefactos de ejecución y datos privados permanecen fuera del árbol. `AGENTS.md` establece las reglas de mantenimiento y evaluación con DSH Standard. Licencia pública pendiente del titular. La preparación para GitHub no equivale a publicación ni a motor completo.
